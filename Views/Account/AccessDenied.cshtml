@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="text-center mt-5">
    <h1 class="display-5 text-danger">Acceso denegado</h1>
    <p class="lead">No tienes permisos para realizar esta acción.</p>
    <p class="text-muted">Contacta a un administrador si crees que es un error.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-primary">Volver al dashboard</a>
</div>